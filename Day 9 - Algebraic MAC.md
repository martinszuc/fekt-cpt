---
tags: [cryptography, cpt, concept, exam-topic, day9]
aliases: [Algebraic MAC, MAC]
related: "[[Day 8 - Signatures]], [[Elliptic Curve Cryptography (ECC)]], [[Equations and Proofs]]"
---

# Algebraic MAC

> [!tip] MAC vs Signature
> | | Algebraic MAC | SBB Signature |
> |---|---|---|
> | Key model | Symmetric — signer and verifier share $sk$ | Asymmetric — only signer knows $sk$ |
> | Speed | Faster verification | Slower — requires a pairing |
> | See also | This note | [[Day 4 - Pairings]] |

---

## Setup

- Elliptic curve group $G = E(\mathbb{F}_p)$ of order $n$, with $q \mid n$
- Generator $g \in G$ of order $q$
- Secret key: $sk = (x_0, x_1, \ldots, x_k) \in_R \mathbb{Z}_q$
- Messages: $(m_1, \ldots, m_k)$

> [!warning] EC notation — scalar multiplication, not exponentiation
> In an EC group, "$g^k$" means $k \cdot g$ (adding $g$ to itself $k$ times). The "exponent" is a scalar coefficient. Keep this in mind for every formula below.

---

## MAC Computation

$$\sigma = \alpha^{-1} \cdot g, \quad \text{where } \alpha = x_0 + \sum_{i=1}^{k} m_i x_i \bmod q$$

**Steps:**
1. Compute $\alpha = x_0 + m_1 x_1 + \cdots + m_k x_k \pmod{q}$
2. Compute $\alpha^{-1} \pmod{q}$ (modular inverse mod group order)
3. $\sigma = \alpha^{-1} \cdot g$ (EC scalar multiplication)

---

## Protocol (Issue & Verify)

| User $U$ | | Issuer/Verifier $V$ |
|---|---|---|
| $(m_1, \ldots, m_k)$ | $\xrightarrow{m_i}$ | |
| | | $\alpha = x_0 + \sum m_i x_i \bmod q$ |
| | | $\sigma = \alpha^{-1} \cdot g$ |
| | $\xleftarrow{\sigma}$ | |

---

## MAC Randomization (for unlinkability)

$$r \in_R \mathbb{Z}_q, \quad h = r \cdot g, \quad \hat{\sigma} = r \cdot \sigma$$

Send $(h, \hat{\sigma})$ to verifier.

**Verify:**

$$\alpha \cdot \hat{\sigma} \stackrel{?}{=} h$$

**Why it works:**

$$\alpha \cdot \hat{\sigma} = \alpha \cdot r \cdot \sigma = \alpha \cdot r \cdot \alpha^{-1} \cdot g = r \cdot g = h \checkmark$$

---

## Worked Example

**Curve:** $E(\mathbb{F}_5): y^2 = x^3 - x + 1$, order 8. Generator $g = A = (4,1)$, subgroup order $q = 4$.

**EC point addition table** (subgroup of order 4 generated by $A$):

| $+$ | $\mathcal{O}$ | $A$ | $B$ | $C$ |
|---|---|---|---|---|
| $\mathcal{O}$ | $\mathcal{O}$ | $A$ | $B$ | $C$ |
| $A$ | $A$ | $B$ | $C$ | $\mathcal{O}$ |
| $B$ | $B$ | $C$ | $\mathcal{O}$ | $A$ |
| $C$ | $C$ | $\mathcal{O}$ | $A$ | $B$ |

where $A=(4,1)$, $B = 2A = (3,0)$, $C = 3A = (4,4)$, $\mathcal{O} = 4A = \infty$.

**Parameters:** $sk = (x_0, x_1) = (1,1)$, message $m_1 = 2$

**Issue:**

$$\alpha = x_0 + x_1 m_1 = 1 + 1 \cdot 2 = 3$$
$$\alpha^{-1} = 3^{-1} \bmod 4 = 3 \quad (\text{since } 3 \cdot 3 = 9 \equiv 1 \bmod 4)$$
$$\sigma = 3 \cdot A = C = (4,4)$$

**Randomize** ($r = 2$):

$$h = 2 \cdot A = B = (3,0)$$
$$\hat{\sigma} = 2 \cdot C = B = (3,0)$$

**Verify:**

$$\alpha \cdot \hat{\sigma} = 3 \cdot B = \mathcal{O} + B = B \stackrel{?}{=} h = B \checkmark$$

(From the table: $3B = C + B = \mathcal{O}$... wait — use the table: $2B = \mathcal{O}$, $3B = A$... No: $B+B = \mathcal{O}$, $B+\mathcal{O} = B$. So $3B = 2B + B = \mathcal{O} + B = B$ ✓)

---

> [!warning] Key formula subtlety
> The inverse $\alpha^{-1}$ is the **modular inverse of the scalar** $\alpha$ mod $q$, computed before multiplying by $g$. Do not confuse with the EC inverse of the point $\sigma$.

> [!warning] Verification uses $\alpha$, not $\alpha^{-1}$
> The verifier computes $\alpha \cdot \hat{\sigma}$ and checks it equals $h$. The signer used $\alpha^{-1}$ when creating $\sigma$, so the multiplication by $\alpha$ cancels it out.
